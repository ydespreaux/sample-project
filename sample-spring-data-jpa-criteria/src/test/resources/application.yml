# Show or not log for each sql query
spring:
  main:
    banner-mode: "off"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:mem:music
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

